# Canonical release evidence

`0.99.1/private-release-runtime-manifest.json` is retained only as historical evidence for the canonical/private Harness release. Its `1524/1524 PASS` statement refers to that private release source and environment. It is not a result produced from this sanitized public tree.

The public tree has a separate reproducible test suite, deterministic evaluation command, and `releases/0.99.1/SHA256SUMS.json` integrity manifest.
